"use strict";
// No users
let userName = ["Eric", "Mike", "tosha", "Ivan", "Tommy"];
if (userName.length === 0) {
    console.log("We need to find some user!");
}
else {
    userName = [];
    console.log("All usernames have been removed. " + userName.length);
}
